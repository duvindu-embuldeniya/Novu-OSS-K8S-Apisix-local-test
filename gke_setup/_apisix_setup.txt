# ── INSTALL ──────────────────────────────────────────────


# Step 1 — Add helm repo 
helm repo add apisix https://apache.github.io/apisix-helm-chart
helm repo update

# Step 2 — Install APISIX
helm install apisix apisix/apisix --create-namespace --namespace dev

# Step 3 — Wait for pods
kubectl get pods -n dev -w



# Step 4 — Install Dashboard
helm install apisix-dashboard apisix/apisix-dashboard --namespace dev --set resources.requests.cpu=50m --set resources.requests.memory=128Mi --set resources.limits.cpu=250m --set resources.limits.memory=512Mi
kubectl port-forward svc/apisix-dashboard 8080:80 -n dev
http://localhost:8080.

(git bash)
kubectl patch svc apisix-gateway -n dev -p '{"spec": {"type": "ClusterIP", "ports": [{"name": "apisix-gateway", "port": 80, "targetPort": 9080, "protocol": "TCP"}]}}'
kubectl get svc apisix-gateway -n dev



# Step 5 — Wait for dashboard pod
kubectl get pods -n dev -w



# ── UNINSTALL ─────────────────────────────────────────────


# Step 1 — Uninstall helm releases
helm uninstall apisix -n dev
helm uninstall apisix-dashboard -n dev

# Step 2 — Delete namespace
kubectl delete namespace dev

# Step 3 — Verify everything gone
kubectl get all -n dev

# Step 4 — Optional: remove helm repo
helm repo remove apisix



